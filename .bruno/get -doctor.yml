info:
  name: get /doctor
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseUrl}}/doctor"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Health checks
    request:
      url: "{{baseUrl}}/doctor"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          [
            {
              "name": "",
              "ok": false,
              "detail": ""
            }
          ]
