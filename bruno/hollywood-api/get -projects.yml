info:
  name: get /projects
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/projects"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: List of projects
    request:
      url: "{{baseUrl}}/projects"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          [
            {
              "id": "878f1f90-a9da-497b-85f8-ce8dddf5989d",
              "title": "THE NEIGHBORHOOD",
              "season": 1,
              "genres": [
                "comedy"
              ],
              "format": "tv",
              "imdbLink": "",
              "posterLink": ""
            }
          ]
